<h3>Спасибо за заказ!</h3>

<b>Детали заказа:</b>
{{ cityFrom.label }} - {{ cityTo.label }}<br/>
{{ dateFrom }} - {{ dateTo }}({{ daysCount }} дней) <br/><br/>

<ul>
    {% for option in options %}
        <li>{{ option.label}} - {{ option.totalPrice}} $</li>
    {% endfor %}
</ul>

<b>Автомобиль:</b><br/>
{{ auto.title }} - {{ auto.price }} $

<b>Итого:</b>
{{ basicPrice * daysCount }}